/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: ["class"],
  content: [
    './pages/**/*.{ts,tsx}',
    './components/**/*.{ts,tsx}',
    './app/**/*.{ts,tsx}',
    './src/**/*.{ts,tsx}',
  ],
  prefix: "",
  theme: {
    container: {
      center: true,
      padding: "2rem",
      screens: {
        "2xl": "1400px",
      },
    },
    extend: {
      colors: {
        border: "hsl(var(--border))",
        input: "hsl(var(--input))",
        ring: "hsl(var(--ring))",
        background: "hsl(var(--background))",
        foreground: "hsl(var(--foreground))",
        primary: {
          DEFAULT: "hsl(var(--primary))",
          foreground: "hsl(var(--primary-foreground))",
        },
        secondary: {
          DEFAULT: "hsl(var(--secondary))",
          foreground: "hsl(var(--secondary-foreground))",
        },
        destructive: {
          DEFAULT: "hsl(var(--destructive))",
          foreground: "hsl(var(--destructive-foreground))",
        },
        muted: {
          DEFAULT: "hsl(var(--muted))",
          foreground: "hsl(var(--muted-foreground))",
        },
        accent: {
          DEFAULT: "hsl(var(--accent))",
          foreground: "hsl(var(--accent-foreground))",
        },
        popover: {
          DEFAULT: "hsl(var(--popover))",
          foreground: "hsl(var(--popover-foreground))",
        },
        card: {
          DEFAULT: "hsl(var(--card))",
          foreground: "hsl(var(--card-foreground))",
        },
        // Envibase 2026 Premium Blue Palette
        "envibase-primary": "#2E5BFF", // Electric Cobalt
        "envibase-bg": "#0A192F",      // Midnight Navy
        "envibase-bg-dark": "#05070A", // Deep Space
        "envibase-surface": "#12213A", // Sub-surface Navy
        "envibase-border": "#1E2D4A",  // Muted Border
        "envibase-emerald": "#00FF88", // Neon Emerald (Status/Utility)
        "high-vis-orange": "#FF5C00",  // For specific annotations
        "hyper-orange": "#FF3D00",
        // Envibase Landing Page & Premium Light Mode
        "landing-primary": "#2E5BFF",
        "landing-paper": "#F8F9FA",
        "landing-navy": "#0A192F",
        "landing-darknavy": "#0d1117",
        "landing-emerald": "#10B981",
        "landing-vibrant": "#00E676",
        // Spatial Context Tokens
        "spatial-zinc": {
          50: "#F8FAFC",
          100: "#F1F5F9",
          200: "#E2E8F0",
          300: "#CBD5E1",
          400: "#94A3B8",
          500: "#64748B",
          600: "#475569",
        }
      },
      borderRadius: {
        lg: "var(--radius)",
        md: "calc(var(--radius) - 2px)",
        sm: "calc(var(--radius) - 4px)",
        xl: "1.5rem",
        "2xl": "2rem",
      },
      fontFamily: {
        sans: ["Space Grotesk", "DM Sans", "Inter", "sans-serif"],
        display: ["Bricolage Grotesque", "sans-serif"],
        mono: ["Geist Mono", "monospace"],
        ui: ["DM Sans", "Inter", "sans-serif"],
      },
      keyframes: {
        "accordion-down": {
          from: { height: "0" },
          to: { height: "var(--radix-accordion-content-height)" },
        },
        "accordion-up": {
          from: { height: "var(--radix-accordion-content-height)" },
          to: { height: "0" },
        },
      },
      animation: {
        "accordion-down": "accordion-down 0.2s ease-out",
        "accordion-up": "accordion-up 0.2s ease-out",
      },
    },
  },
  plugins: [require("tailwindcss-animate")],
}